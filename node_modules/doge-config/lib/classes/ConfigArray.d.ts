import { ConfigField } from '../classes/ConfigField';
import * as types from '../types';
export declare class ConfigArray extends Array<types.ValidConfigValue> {
    #private;
    constructor(field: ConfigField);
    __save(): void;
}
export default ConfigArray;
