#!/usr/bin/env node
'use strict';
Object.defineProperty(exports, '__esModule', { value: true });
const _1 = require('.');
const chunk_size = 80;
const filter = /[^\w~]/g;
let buffer = String();
function flush() {
	let n = 0;
	for (let i = chunk_size; i < buffer.length; i += chunk_size) {
		process.stdout.write((0, _1.decode)(buffer.slice(i - chunk_size, i)));
		n = i;
	}
	buffer = buffer.slice(n - chunk_size);
}
process.stdin.on('data', (chunk) => {
	buffer += String(chunk).replace(filter, '');
	flush();
});
process.stdin.on('end', () => {
	process.stdout.write((0, _1.decode)(buffer));
});
