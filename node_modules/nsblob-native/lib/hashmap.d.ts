import { Semaphore } from '@prokopschield/semaphore';
import { DB } from 'insta-db';
export declare class HashMap extends DB {
	semaphore: Semaphore;
	constructor();
	setB2H(blake: string, hash: string): boolean | undefined;
	getB2H(blake: string): string | undefined;
}
