'use strict';
Object.defineProperty(exports, '__esModule', { value: true });
exports.cacheFn = void 0;
function cacheFn(fn, thisArg) {
	const cache = new Map();
	const cfn = (x) => {
		const cache_val = cache.get(x);
		if (cache_val !== undefined) {
			return cache_val;
		} else {
			const y = fn.call(thisArg, x);
			if (y !== undefined) {
				cache.set(x, y);
			}
			return y;
		}
	};
	return cfn;
}
exports.cacheFn = cacheFn;
exports.default = cacheFn;
Object.defineProperties(cacheFn, {
	default: { get: () => cacheFn },
	cacheFn: { get: () => cacheFn },
});
